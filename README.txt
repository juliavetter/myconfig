Just some config files that it's useful to have in one place.

I recommend (more to myself than you) to hard link the each config file to its location as read by the corresponding program. 
E.g. for .bashrc one should do:
ln <myconfig path>/.bashrc ~/.bashrc
